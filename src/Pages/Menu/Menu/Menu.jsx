import React from 'react';
import { Helmet } from 'react-helmet';

const Menu = () => {
    return (
        <div>
            <Helmet>
                <title>Bistro / Menu</title>
            </Helmet>
        </div>
    );
};

export default Menu;